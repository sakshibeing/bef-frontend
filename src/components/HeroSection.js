import React from 'react';
import './HeroSection.css'; // Import CSS here

const HeroSection = () => (
  <div className="hero-section">
    <h1>Latest Updates from BEF</h1>
    <p>See what’s happening at our Conclaves, Sprints, and in media coverage.</p>
  </div>
);

export default HeroSection;
